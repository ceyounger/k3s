apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jackett-longhorn-pvc
  namespace: media-services
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
