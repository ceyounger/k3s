apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: oauth-longhorn-pvc
  namespace: general-services
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
